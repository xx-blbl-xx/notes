### es的分片数量在索引创建时就指定了（默认5分片1副本），通常es使用默认使用_id或指定的字段进行哈希然后根据分片数取模，查询时则是查询全部分片（由于读可以走副本所以可以水平扩展）

* 每个分片实际都是一个Lucence索引，处理各自的一部分数据
* 如果需要重新配置分片数量必须重新新建索引，数据重新同步，然后通过别名配置进行无痛切换
* 由于是es是文档数据库，对字段限制相当宽松，即便是多个索引使用相同别名，在搜索时只有能搜到就会返回对应数据
* 业务通常根据时间水平分表建立多个索引，查询时指定时间确定索引范围

